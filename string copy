#include <stdio.h>
#include <string.h>

void main()
{
  char str[100];
  char desrt[15];  
  clrscr();
  printf("enter the string:");
  scanf("%s",str);
  printf("enter the string:");
  scanf("%s",desrt);
  printf("before coping:%s\n",str);
  printf("after string:%s\n", desrt);
  strcpy(str,desrt);
  printf("before coping:%s\n",str);
  printf("after string:%s\n", desrt);
  
  getch();
}
